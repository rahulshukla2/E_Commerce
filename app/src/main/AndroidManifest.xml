<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.e_comm" >

    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.E_comm" >
        <activity
            android:name=".SellerHomeActivity"
            android:label="@string/title_activity_seller_home"
            android:theme="@style/Theme.E_comm.NoActionBar" >
        </activity>
        <activity
            android:name=".RegisterActivity1"
            android:label="@string/title_activity_register1"
            android:theme="@style/Theme.E_comm.NoActionBar" />
        <activity
            android:name=".CustomHomeActivity"
            android:label="@string/title_activity_custom_home"
            android:theme="@style/Theme.E_comm.NoActionBar" />
        <activity android:name=".StartActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AdminHomeActivity"
            android:label="AdminHomeActivity"
            android:theme="@style/Theme.E_comm.NoActionBar" >
        </activity>
        <activity android:name=".RegisterActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".OTPActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>